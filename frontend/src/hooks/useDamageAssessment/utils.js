// src/hooks/useDamageAssessment/utils.js
export const validateAssessment = (assessment) => {
    // Add validation logic for each assessment.
    // Return true if valid, false if invalid.
    return true;
  };
  
  export const prepareAssessmentForSave = (assessment) => {
    // Transform assessment data as needed for the API
    return { ...assessment };
  };
  